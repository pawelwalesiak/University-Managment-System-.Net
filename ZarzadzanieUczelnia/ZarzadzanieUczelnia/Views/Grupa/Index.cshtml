@model IEnumerable<ZarzadzanieUczelnia.Models.Grupa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Utwórz nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nauczyciel.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Semestr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nauczyciel.Imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Semestr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edycja", "Edit", new { id=item.GrupaID }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.GrupaID }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.GrupaID })
        </td>
    </tr>
}

</table>
